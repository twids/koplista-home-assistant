language: "en"
intents:
  KoplistaAddItem:
    data:
      - sentences:
          - "add {item} to the shopping list"
          - "add {item} to the grocery list"
          - "put {item} on the shopping list"
          - "buy {item}"
          - "we need {item}"
          - "get {item}"
